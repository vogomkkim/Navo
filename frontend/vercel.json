{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install --platform=linux --arch=x64",
  "buildCommand": "npm run build",
  "outputDirectory": "out",
  "functions": {
    "app/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
